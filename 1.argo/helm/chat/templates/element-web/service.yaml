{{- if .Values.elementWeb.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: element
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: element
  ports:
    - name: default
      protocol: TCP
      port: 8080
      targetPort: 8080
{{- end -}}
