apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: terra-vault
  namespace: argocd
spec:
  project: default
  source:
    path: 1.argo/terra/vault
    repoURL: "{{ .Values.repo }}"
    targetRevision: HEAD
  destination:
    namespace: vault
    server: "https://kubernetes.default.svc"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
