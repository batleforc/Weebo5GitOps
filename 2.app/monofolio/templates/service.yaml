{{- if .Values.enabled -}}
---
apiVersion: v1
kind: Service
metadata:
  name: monofolio
  labels:
    app: monofolio
spec:
  selector:
    app: monofolio
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
