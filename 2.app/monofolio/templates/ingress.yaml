{{- if .Values.enabled -}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monofolio
  labels:
    app: monofolio
  annotations:
    cert-manager.io/cluster-issuer: "{{ .Values.ingress.certIssuer }}"
spec:
  rules:
    {{- range .Values.domain }}
    - host: {{ .name }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monofolio
                port:
                  number: 3000
    {{- end }}
  tls:
    {{- range .Values.domain }}
    - hosts:
        - {{ .name }}
      secretName: {{ .cert }}
    {{- end }}
{{- end -}}
